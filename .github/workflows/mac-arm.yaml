name: test opencv build on macos

on:
  workflow_dispatch:
  schedule:
    - cron: '0 * * * *'

jobs:
  run-ci:
    runs-on: macos-26
    steps:
    - name: Checkout Forked OpenCV
      uses: actions/checkout@v4
      with:
        repository: 'habib-source/opencv'
        ref: 'FindPython'
    - name: Purge Python Framework Versions
      run: sudo rm -rf /Library/Frameworks/Python.framework/Versions/*
    - name: check that python eex exist
      run: brew uninstall --ignore-dependencies python3

    - name: check that python eex exist
      run: sudo rm -rf /opt/homebrew/Frameworks/Python.framework/
    - name: Verify Environment
      run: |
        which python3
        xcodebuild -version
    - name: install numpy
      run: python3 -m pip install numpy
    - name: Run OpenCV CMake Configuration
      run: cmake -B build -DBUILD_opencv_python3=ON -DBUILD_opencv_java=OFF -DINSTALL_CREATE_DISTRIB=ON -DBUILD_TESTS=OFF -DBUILD_PERF_TESTS=OFF -DBUILD_DOCS=OFF -DPython3_LIMITED_API=ON -DBUILD_OPENEXR=ON -DOPENCV_Python3_INSTALL_PATH=python
